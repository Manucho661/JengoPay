import { operatingOutFlow } from "./api/operatingOutFlow.js";

document.addEventListener("DOMContentLoaded", async () => {
    // Execute in sequence
        console.log('yoyo');

    await operatingOutFlow();
});